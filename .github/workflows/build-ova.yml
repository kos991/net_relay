name: build-ova

on:
  push:
    tags:
      - "v1.*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Stable release tag; empty means the next v1.0.N tag"
        required: false
        default: ""
      netbird_relay_ref:
        description: "NetBird upstream ref/tag/branch to build relay from"
        required: false
        default: "main"
      publish_release:
        description: "Publish GitHub Release after build"
        required: false
        default: "true"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ALPINE_IMAGE_URL: https://dl-cdn.alpinelinux.org/alpine/v3.23/releases/cloud/nocloud_alpine-3.23.4-x86_64-bios-cloudinit-r0.qcow2
      OVA_NAME: net-relay-alpine-x86_64.ova
      OVA_MINIMAL_NAME: net-relay-alpine-x86_64-minimal.ova
      RAW_NAME: net-relay-alpine-x86_64.raw.img.gz
      VHD_NAME: net-relay-alpine-x86_64.vhd.gz
      QCOW2_NAME: net-relay-alpine-x86_64.qcow2.gz
      INSTALLER_ARCHIVE_NAME: net_relay-main.tar.gz
      RELAY_AMD64_PACKAGE: netbird-relay-linux-amd64.tar.gz
      RELAY_ARM64_PACKAGE: netbird-relay-linux-arm64.tar.gz
      NETBIRD_RELAY_REF: ${{ github.event.inputs.netbird_relay_ref || '' }}
      OVA_APK_PACKAGES: bash ca-certificates curl sudo wget openssl openssh-server cloud-init cloud-init-openrc qemu-guest-agent qemu-guest-agent-openrc procps-ng util-linux iproute2 cloud-utils-growpart e2fsprogs-extra xfsprogs btrfs-progs zram-init cronie parted tzdata gnutls acme.sh
      TRIVY_VERSION: "0.71.0"
      TRIVY_CHECKSUM: "30a3d22b23f88c233f1658f562fb477cae3b3e8b4761109d515b7698daf85814"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check release scripts
        run: bash tests/release-check.sh

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.8"
          cache: false

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-utils libguestfs-tools tar gzip

      - name: Install Trivy
        run: |
          curl -fsSL "https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_Linux-64bit.tar.gz" -o trivy.tar.gz
          printf '%s  trivy.tar.gz\n' "${TRIVY_CHECKSUM}" | sha256sum -c
          tar -xzf trivy.tar.gz trivy
          sudo install -m 0755 trivy /usr/local/bin/trivy

      - name: Resolve NetBird relay ref
        run: |
          if [ -z "${NETBIRD_RELAY_REF}" ]; then
            NETBIRD_RELAY_REF="$(cat .github/netbird-relay-upstream.sha)"
          fi
          if ! printf '%s' "${NETBIRD_RELAY_REF}" | grep -Eq '^v[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "::error::NetBird relay ref must be an official semantic release tag"
            exit 1
          fi
          echo "NETBIRD_RELAY_REF=${NETBIRD_RELAY_REF}" >> "$GITHUB_ENV"

      - name: Build official NetBird relay binaries
        run: |
          mkdir -p dist release
          TARGET_OS=linux TARGET_ARCH=amd64 NETBIRD_RELAY_REF="${NETBIRD_RELAY_REF}" bash packaging/relay/build-relay.sh
          TARGET_OS=linux TARGET_ARCH=arm64 NETBIRD_RELAY_REF="${NETBIRD_RELAY_REF}" bash packaging/relay/build-relay.sh
          TARGET_OS=linux TARGET_ARCH=amd64 NETBIRD_RELAY_REF="${NETBIRD_RELAY_REF}" bash packaging/relay/package-relay.sh
          TARGET_OS=linux TARGET_ARCH=arm64 NETBIRD_RELAY_REF="${NETBIRD_RELAY_REF}" bash packaging/relay/package-relay.sh

      - name: Scan relay source with govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          (cd .build/netbird && govulncheck ./relay)

      - name: Generate release metadata
        id: meta
        env:
          RELEASE_TAG_INPUT: ${{ github.event.inputs.release_tag || '' }}
        run: |
          if [ "${GITHUB_REF_TYPE:-}" = "tag" ]; then
            release_tag="${GITHUB_REF_NAME}"
          elif [ -n "${RELEASE_TAG_INPUT}" ]; then
            release_tag="${RELEASE_TAG_INPUT}"
          else
            git fetch --tags --force
            tag_prefix="v1.0"
            latest_patch="$(git tag -l "${tag_prefix}.*" | sed -n "s/^${tag_prefix//./\\.}\\.\\([0-9][0-9]*\\)$/\\1/p" | sort -n | tail -1)"
            if [ -z "${latest_patch}" ]; then
              release_tag="${tag_prefix}.0"
            else
              release_tag="${tag_prefix}.$((latest_patch + 1))"
            fi
          fi

          if ! printf '%s' "${release_tag}" | grep -Eq '^v1\.0\.[0-9]+$'; then
            echo "::error::Stable releases must use v1.0.N tags, for example v1.0.31"
            exit 1
          fi

          root_password="Net@rels2026"
          ssh_port="$(python3 -c "import secrets; print(secrets.randbelow(45536) + 20000)")"

          {
            echo "release_tag=${release_tag}"
            echo "release_channel=stable"
            echo "root_password=${root_password}"
            echo "ssh_port=${ssh_port}"
            echo "update_note=$(git log -1 --pretty=%s)"
          } >> "$GITHUB_OUTPUT"

      - name: Prepare installer payload
        run: |
          mkdir -p release
          mkdir -p ova-build/netbird-relay-installer ova-build/relay-package
          cp scripts/installer/install.sh ova-build/netbird-relay-installer/install.sh
          cp scripts/installer/setup-relay.sh ova-build/netbird-relay-installer/setup-relay.sh
          cp scripts/certificate/reload-relay-certificate.sh ova-build/netbird-relay-installer/reload-relay-certificate.sh
          cp README.md ova-build/netbird-relay-installer/README.md
          tar -xzf "release/${RELAY_AMD64_PACKAGE}" -C ova-build/relay-package
          git archive --format=tar.gz --prefix=net_relay-main/ HEAD -o "release/${INSTALLER_ARCHIVE_NAME}"
          cp main.sh release/main.sh
          cp scripts/installer/install.sh release/install.sh
          cp scripts/installer/setup-relay.sh release/setup-relay.sh
          cp scripts/certificate/reload-relay-certificate.sh release/reload-relay-certificate.sh

      - name: Download Alpine cloud image
        run: |
          curl -fsSL "$ALPINE_IMAGE_URL" -o ova-build/alpine.qcow2
          qemu-img resize ova-build/alpine.qcow2 3G
          sudo guestfish --rw -a ova-build/alpine.qcow2 <<'EOF'
          run
          resize2fs /dev/sda
          EOF

      - name: Fetch Alpine OVA runtime packages
        run: |
          mkdir -p ova-build/apks
          BASE_APK_PACKAGES="$(sudo virt-cat -a ova-build/alpine.qcow2 /lib/apk/db/installed \
            | awk -F: '$1 == "P" {print $2}' \
            | sort -u \
            | tr '\n' ' ')"
          [[ -n "${BASE_APK_PACKAGES}" ]] || {
            echo "::error::Unable to read the Alpine base package database"
            exit 1
          }
          docker run --rm \
            -e "OVA_APK_PACKAGES=${OVA_APK_PACKAGES}" \
            -e "BASE_APK_PACKAGES=${BASE_APK_PACKAGES}" \
            -v "$PWD/ova-build/apks:/apks" \
            alpine:3.23 \
            sh -ec 'apk update && cd /apks && apk fetch --recursive ${OVA_APK_PACKAGES} ${BASE_APK_PACKAGES}'

      - name: Customize Alpine OVA image
        env:
          LIBGUESTFS_BACKEND: direct
          ROOT_PASSWORD: ${{ steps.meta.outputs.root_password }}
          SSH_PORT: ${{ steps.meta.outputs.ssh_port }}
        run: |
          set +e
          sudo -E virt-customize --no-network --format qcow2 -a ova-build/alpine.qcow2 \
            --root-password "password:${ROOT_PASSWORD}" \
            --mkdir /usr/local/bin \
            --mkdir /usr/local/sbin \
            --mkdir /etc/init.d \
            --mkdir /etc/ssh \
            --mkdir /etc/profile.d \
            --mkdir /etc/modules-load.d \
            --mkdir /etc/sysctl.d \
            --copy-in ova-build/apks:/tmp \
            --copy-in ova-build/relay-package/netbird-relay/bin/netbird-relay:/usr/local/bin \
            --copy-in ova-build/relay-package/netbird-relay/services/netbird-relay.openrc:/etc/init.d \
            --copy-in ova-build/netbird-relay-installer:/opt \
            --mkdir /usr/local/libexec/netbird-relay \
            --copy-in scripts/certificate/reload-relay-certificate.sh:/usr/local/libexec/netbird-relay \
            --copy-in packaging/ova/files/rels:/usr/local/sbin \
            --copy-in packaging/ova/files/net-relay-firstboot:/usr/local/sbin \
            --copy-in packaging/ova/files/setup-root-resize:/usr/local/sbin \
            --copy-in packaging/ova/files/setup-network-check:/usr/local/sbin \
            --copy-in packaging/ova/files/setup-zram:/usr/local/sbin \
            --copy-in packaging/ova/files/setup-kernel-tuning:/usr/local/sbin \
            --copy-in packaging/ova/files/sshd_config:/etc/ssh \
            --copy-in packaging/ova/files/00-net-relay-locale.sh:/etc/profile.d \
            --copy-in packaging/ova/files/virtio-modules.conf:/etc/modules-load.d \
            --copy-in packaging/ova/files/98-net-relay-datasources.cfg:/etc/cloud/cloud.cfg.d \
            --copy-in packaging/ova/files/99-net-relay-login.cfg:/etc/cloud/cloud.cfg.d \
            --copy-in packaging/ova/files/99-net-relay-sysctl.conf:/etc/sysctl.d \
            --copy-in packaging/ova/files/root-profile:/root \
            --run-command "apk add --no-network --allow-untrusted --upgrade /tmp/apks/*.apk && rm -rf /tmp/apks" \
            --run-command "ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime && printf '%s\n' 'Asia/Shanghai' > /etc/timezone" \
            --run-command "chmod 0644 /etc/profile.d/00-net-relay-locale.sh" \
            --run-command "chage -d 0 root || passwd -e root || true" \
            --run-command "sed -i 's/__SSH_PORT__/${SSH_PORT}/' /etc/ssh/sshd_config" \
            --run-command "sed -i 's/__SSH_PORT__/${SSH_PORT}/' /usr/local/sbin/net-relay-firstboot" \
            --run-command "chmod +x /usr/local/bin/netbird-relay /usr/local/libexec/netbird-relay/reload-relay-certificate.sh /usr/local/sbin/rels /usr/local/sbin/net-relay-firstboot /usr/local/sbin/setup-root-resize /usr/local/sbin/setup-network-check /usr/local/sbin/setup-zram /usr/local/sbin/setup-kernel-tuning /opt/netbird-relay-installer/install.sh /opt/netbird-relay-installer/setup-relay.sh" \
            --run-command "ln -sf /usr/local/sbin/rels /usr/local/bin/rels" \
            --run-command "setup-cloud-init" \
            --run-command "rc-update add qemu-guest-agent default || true" \
            --run-command "chmod +x /etc/init.d/netbird-relay.openrc && mv /etc/init.d/netbird-relay.openrc /etc/init.d/netbird-relay" \
            --run-command "cat /root/root-profile >> /root/.profile && rm -f /root/root-profile" 2>&1 | tee ova-build/virt-customize.log
          status="${PIPESTATUS[0]}"
          if [ "$status" -ne 0 ]; then
            {
              echo "virt-customize failed with exit code ${status}"
              tail -n 120 ova-build/virt-customize.log
            } | python3 -c 'import sys; print("::error::" + sys.stdin.read().replace("%", "%25").replace("\r", "%0D").replace("\n", "%0A"))'
            exit "$status"
          fi

      - name: Verify OVA SSH port
        env:
          EXPECTED_SSH_PORT: ${{ steps.meta.outputs.ssh_port }}
        run: |
          actual_ssh_port="$(sudo virt-cat -a ova-build/alpine.qcow2 /etc/ssh/sshd_config | awk '$1 == "Port" {print $2; exit}')"
          if [[ ! "$actual_ssh_port" =~ ^[0-9]+$ ]]; then
            echo "::error::OVA sshd_config does not contain a numeric SSH port"
            exit 1
          fi
          if [[ "$actual_ssh_port" != "$EXPECTED_SSH_PORT" ]]; then
            echo "::error::OVA SSH port mismatch: expected ${EXPECTED_SSH_PORT}, image contains ${actual_ssh_port}"
            exit 1
          fi
          firstboot_ssh_port="$(sudo virt-cat -a ova-build/alpine.qcow2 /usr/local/sbin/net-relay-firstboot \
            | sed -nE 's/^- SSH (port|端口)[:：][[:space:]]*//p' \
            | head -n 1)"
          if [[ "$firstboot_ssh_port" != "$actual_ssh_port" ]]; then
            echo "::error::OVA first-boot SSH port mismatch: sshd=${actual_ssh_port}, firstboot=${firstboot_ssh_port}"
            exit 1
          fi
          echo "SSH_PORT=${actual_ssh_port}" >> "$GITHUB_ENV"

      - name: Package release images
        run: |
          qemu-img convert -p -O vmdk -o subformat=streamOptimized,adapter_type=lsilogic ova-build/alpine.qcow2 release/net-relay-alpine-x86_64.vmdk
          vmdk_size="$(stat -c%s release/net-relay-alpine-x86_64.vmdk)"
          write_ovf() {
            local ovf_path="$1"
            local network_connection="$2"
            cat > "$ovf_path" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <Envelope xmlns="http://schemas.dmtf.org/ovf/envelope/1" xmlns:ovf="http://schemas.dmtf.org/ovf/envelope/1" xmlns:rasd="http://schemas.dmtf.org/wbem/wscim/1/cim-schema/2/CIM_ResourceAllocationSettingData" xmlns:vmw="http://www.vmware.com/schema/ovf">
            <References>
              <File ovf:id="file1" ovf:href="net-relay-alpine-x86_64.vmdk" ovf:size="${vmdk_size}"/>
            </References>
            <DiskSection>
              <Info>Virtual disk information</Info>
              <Disk ovf:diskId="disk1" ovf:fileRef="file1" ovf:capacity="3" ovf:capacityAllocationUnits="byte * 2^30" ovf:format="http://www.vmware.com/interfaces/specifications/vmdk.html#streamOptimized"/>
            </DiskSection>
            <NetworkSection>
              <Info>Logical networks used in the package</Info>
              <Network ovf:name="VM Network"><Description>VM Network</Description></Network>
            </NetworkSection>
            <VirtualSystem ovf:id="net-relay-alpine">
              <Info>NetBird Relay Alpine OVA</Info>
              <Name>net-relay-alpine</Name>
              <OperatingSystemSection ovf:id="94" vmw:osType="otherlinux64Guest"><Info>Alpine Linux</Info></OperatingSystemSection>
              <VirtualHardwareSection>
                <Info>Virtual hardware requirements</Info>
                <Item><rasd:Description>1 virtual CPU</rasd:Description><rasd:ElementName>1 virtual CPU</rasd:ElementName><rasd:InstanceID>1</rasd:InstanceID><rasd:ResourceType>3</rasd:ResourceType><rasd:VirtualQuantity>1</rasd:VirtualQuantity></Item>
                <Item><rasd:AllocationUnits>byte * 2^20</rasd:AllocationUnits><rasd:Description>1024MB memory</rasd:Description><rasd:ElementName>1024MB memory</rasd:ElementName><rasd:InstanceID>2</rasd:InstanceID><rasd:ResourceType>4</rasd:ResourceType><rasd:VirtualQuantity>1024</rasd:VirtualQuantity></Item>
                <Item><rasd:Description>SCSI Controller</rasd:Description><rasd:ElementName>SCSI Controller 0</rasd:ElementName><rasd:InstanceID>3</rasd:InstanceID><rasd:ResourceSubType>lsilogic</rasd:ResourceSubType><rasd:ResourceType>6</rasd:ResourceType></Item>
                <Item><rasd:AddressOnParent>0</rasd:AddressOnParent>${network_connection}<rasd:Description>E1000 network adapter</rasd:Description><rasd:ElementName>Ethernet 1</rasd:ElementName><rasd:InstanceID>4</rasd:InstanceID><rasd:ResourceSubType>E1000</rasd:ResourceSubType><rasd:ResourceType>10</rasd:ResourceType></Item>
                <Item><rasd:Description>Disk</rasd:Description><rasd:ElementName>Disk 1</rasd:ElementName><rasd:HostResource>ovf:/disk/disk1</rasd:HostResource><rasd:InstanceID>5</rasd:InstanceID><rasd:Parent>3</rasd:Parent><rasd:ResourceType>17</rasd:ResourceType></Item>
              </VirtualHardwareSection>
            </VirtualSystem>
          </Envelope>
          EOF
          }
          write_ovf release/net-relay-alpine-x86_64.ovf '<rasd:Connection>VM Network</rasd:Connection>'
          write_ovf release/net-relay-alpine-x86_64-minimal.ovf ''
          tar -C release -cf "release/${OVA_NAME}" net-relay-alpine-x86_64.ovf net-relay-alpine-x86_64.vmdk
          tar -C release -cf "release/${OVA_MINIMAL_NAME}" net-relay-alpine-x86_64-minimal.ovf net-relay-alpine-x86_64.vmdk
          qemu-img convert -p -O raw ova-build/alpine.qcow2 release/net-relay-alpine-x86_64.raw.img
          qemu-img convert -p -O vpc ova-build/alpine.qcow2 release/net-relay-alpine-x86_64.vhd
          qemu-img convert -p -O qcow2 -o compat=1.1 ova-build/alpine.qcow2 release/net-relay-alpine-x86_64.qcow2
          qemu-img info --output=json release/net-relay-alpine-x86_64.vhd | jq -e '.format == "vpc"'
          qemu-img check release/net-relay-alpine-x86_64.qcow2
          gzip -9 release/net-relay-alpine-x86_64.raw.img
          gzip -9 release/net-relay-alpine-x86_64.vhd
          gzip -9 release/net-relay-alpine-x86_64.qcow2

      - name: Scan release workspace with Trivy
        run: |
          mkdir -p ova-build/trivy-workspace
          cp -a .github packaging scripts worker install.sh main.sh setup-relay.sh README.md LICENSE ova-build/trivy-workspace/
          cp -a release ova-build/trivy-workspace/
          trivy fs \
            --scanners vuln,secret,misconfig \
            --severity CRITICAL,HIGH \
            --ignore-unfixed \
            --ignorefile .trivyignore.yaml \
            --exit-code 1 \
            --format json \
            --output release/trivy-fs.json \
            ova-build/trivy-workspace

      - name: Scan Alpine rootfs with Trivy
        env:
          LIBGUESTFS_BACKEND: direct
        run: |
          mkdir -p ova-build/rootfs
          cleanup_rootfs() {
            rm -rf ova-build/rootfs
          }
          trap cleanup_rootfs EXIT
          sudo virt-copy-out -a ova-build/alpine.qcow2 / ova-build/rootfs
          sudo chown -R "$(id -u):$(id -g)" ova-build/rootfs
          trivy rootfs \
            --scanners vuln \
            --pkg-types os \
            --severity CRITICAL,HIGH \
            --ignore-unfixed \
            --exit-code 0 \
            --format json \
            --output release/trivy-rootfs.json \
            ova-build/rootfs
          if python3 -c 'import json,sys; data=json.load(open("release/trivy-rootfs.json")); sys.exit(0 if any(r.get("Vulnerabilities") for r in data.get("Results", [])) else 1)'; then
            trivy rootfs \
              --scanners vuln \
              --pkg-types os \
              --severity CRITICAL,HIGH \
              --ignore-unfixed \
              --exit-code 0 \
              --format table \
              ova-build/rootfs
            exit 1
          fi

      - name: Generate release SBOM
        run: |
          trivy filesystem \
            --format cyclonedx \
            --output release/net-relay-sbom.cdx.json \
            release
          trivy sbom \
            --severity CRITICAL,HIGH \
            --ignore-unfixed \
            --exit-code 1 \
            release/net-relay-sbom.cdx.json
          (cd release && sha256sum "$OVA_NAME" "$OVA_MINIMAL_NAME" "$RAW_NAME" "$VHD_NAME" "$QCOW2_NAME" "$INSTALLER_ARCHIVE_NAME" "$RELAY_AMD64_PACKAGE" "$RELAY_ARM64_PACKAGE" main.sh install.sh setup-relay.sh reload-relay-certificate.sh trivy-rootfs.json trivy-fs.json net-relay-sbom.cdx.json > SHA256SUMS)

      - name: Write release notes
        run: |
          cat > release/RELEASE_NOTES.md <<EOF
          # NetBird Relay ${{ steps.meta.outputs.release_tag }}

          This release provides official NetBird Relay binaries built from the upstream NetBird source, plus ready-to-import OVA, RAW, VHD, and QCOW2 images.

          ## Quick install
          \`\`\`bash
          curl -sSL https://rels.jinfei.org | sh
          \`\`\`

          ## Release channel

          - Channel: stable (`v1.0.N` only).
          - NetBird Relay upstream ref: ${NETBIRD_RELAY_REF}

          ## Published assets

          - ${RELAY_AMD64_PACKAGE}: Linux amd64 official relay binary package.
          - ${RELAY_ARM64_PACKAGE}: Linux arm64 official relay binary package.
          - ${OVA_NAME}: OVA with OVF network Connection for importers that require it.
          - ${OVA_MINIMAL_NAME}: OVA without OVF network Connection for strict importers that reject it.
          - ${RAW_NAME}: RAW image for cloud import workflows such as Aliyun custom images.
          - ${VHD_NAME}: VHD image for cloud import workflows such as Aliyun custom images.
          - ${QCOW2_NAME}: Standalone QCOW2 v1.1 image for cloud import workflows such as Aliyun custom images.
          - main.sh: secure bootstrap script served by https://rels.jinfei.org.
          - install.sh: one-click installer for Debian, Ubuntu, Rocky, Alma, and Alpine.
          - setup-relay.sh: binary Relay configuration wizard.
          - reload-relay-certificate.sh: certificate validation and Relay restart hook.
          - SHA256SUMS: SHA256 checksums for all published artifacts.
          - trivy-rootfs.json, trivy-fs.json, net-relay-sbom.cdx.json: vulnerability scan reports and CycloneDX SBOM.

          ## OVA access

          - User: root
          - Initial password: ${{ steps.meta.outputs.root_password }}
          - SSH port: ${SSH_PORT}
          - Login command: \`ssh -p ${SSH_PORT} root@<OVA_IP>\`
          - First login will force a root password change before initialization continues.

          ## OVA initialization

          - Enables ZRAM swap and daily ZRAM compact maintenance.
          - Applies the conservative NetBird Relay kernel tuning profile.
          - Expands the root partition when the virtual disk has been resized.
          - Runs DHCP and network preflight checks on first boot.
          - Installs cloud-init, qemu-guest-agent, VirtIO support, xfsprogs, and btrfs-progs.
          - Run \`rels\` and choose option 7 to view ZRAM and kernel optimization status.

          ## Security and verification

          - Relay service runs as the dedicated netbird-relay user in binary mode.
          - Release build runs govulncheck and Trivy High/Critical gates before publishing.
          - Verify downloaded artifacts with SHA256SUMS before production use.
          - Update: ${{ steps.meta.outputs.update_note }}
          EOF

      - name: Upload unpublished OVA test artifacts
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.publish_release == 'false' }}
        uses: actions/upload-artifact@v4
        with:
          name: net-relay-ova-test-${{ github.run_id }}
          retention-days: 7
          path: |
            release/net-relay-alpine-x86_64.ova
            release/net-relay-alpine-x86_64-minimal.ova
            release/net-relay-alpine-x86_64.raw.img.gz
            release/net-relay-alpine-x86_64.vhd.gz
            release/net-relay-alpine-x86_64.qcow2.gz
            release/SHA256SUMS
            release/RELEASE_NOTES.md

      - name: Publish release
        if: ${{ github.event_name != 'workflow_dispatch' || github.event.inputs.publish_release != 'false' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${{ steps.meta.outputs.release_tag }}"
          if [ "${GITHUB_REF_TYPE:-}" != "tag" ]; then
            git tag "$tag"
            git push origin "$tag"
          fi
          gh release create "$tag" \
            --title "$tag" \
            --notes-file release/RELEASE_NOTES.md \
            "release/${INSTALLER_ARCHIVE_NAME}" \
            "release/${OVA_NAME}" \
            "release/${OVA_MINIMAL_NAME}" \
            "release/${RAW_NAME}" \
            "release/${VHD_NAME}" \
            "release/${QCOW2_NAME}" \
            "release/${RELAY_AMD64_PACKAGE}" \
            "release/${RELAY_ARM64_PACKAGE}" \
            "release/main.sh" \
            "release/install.sh" \
            "release/setup-relay.sh" \
            "release/reload-relay-certificate.sh" \
            release/trivy-rootfs.json \
            release/trivy-fs.json \
            release/net-relay-sbom.cdx.json \
            release/SHA256SUMS
